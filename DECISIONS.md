# DECISIONS

> Append-only log of every resolved decision: what, when, why. Newest at the bottom. One row per decision; never edit a past row — supersede it with a new one that references it.
>
> Per `EXECUTION_STANDARDS.md` §6, every user-confirmed decision is appended here in the same commit that acts on it. Open items live in `PROJECT_SPEC.md` §17 until resolved, then graduate to a row here.

## Locked (pre-code)

| # | Date | Decision | Rationale | Supersedes |
|---|---|---|---|---|
| 1 | 2026-06 | **Runtime: TypeScript / Node 22** | Stagehand is TS-native (the Python port lags); matches the Kompliant MCP-server stack; the AI sits behind the Claude API so there's no ML-library pull toward Python. | — |
| 2 | 2026-06 | **Store: PostgreSQL everywhere** for run records + playbook index | One mental model across local and cloud; transactional `active_version` pointer moves; matches existing Postgres expertise. Playbook *bodies* and evidence stay as blobs in local FS / S3. | — |
| 3 | 2026-06 | **Replay extraction: structural-first with a surfaced, config-gated LLM fallback** | Resilience to minor layout shifts without a full heal, but never a silent rescue — `meta.llm_fallback_used` + per-playbook counting keep drift visible. | — |
| 4 | 2026-06 | **Concurrency: three per-container limits** (`MAX_CONCURRENT_RUNS` / `MAX_QUEUE_DEPTH` / `RUN_TIMEOUT_SECONDS`) + full request-isolation invariants | Separates resource gating, backpressure, and stuck-run recovery — conflating them causes OOM or silent drops. Global cross-container cap deferred to v2. | — |
| 5 | 2026-06 | **Playbooks are declarative JSON ops, interpreted — never generated code** | Safety (no `eval` of LLM output), diffable across versions, exact provenance-based parameterization, portable. Cost: an op vocabulary to maintain. | — |

## Recorded defaults (open in spec §17, defaulted so Phase 1 isn't blocked — confirm or change at Phase 0)

| # | Date | Question | Recorded default | Notes |
|---|---|---|---|---|
| D1 | 2026-06 | API auth mode for v1 | `none` (internal network trust) for first deploy; `hmac` (KSig1-style) reserved | Must be a conscious decision before anything public-facing. |
| D2 | 2026-06 | Synchronous `?wait=true` mode | **out** for v1 (async-only) | Revisit if callers need fast inline playbook replays. |
| D3 | 2026-06 | Project name | TBD — replaces "engine" placeholder | Blocks package name, ID prefixes, image name. Decide at Phase 0. |
| D4 | 2026-06 | ID scheme | ULID/KSUID, prefixed `run_` / `pb_` | Sortable, no coordination. |

## Resolved during build

> (Append rows here as phases resolve open questions or make as-built decisions.)

| # | Date | Decision | Rationale | Phase | Supersedes |
|---|---|---|---|---|---|
| 6 | 2026-06-16 | **Project name: `Rote`** — replaces the "engine" placeholder | "Learn once, replay by rote" captures the two-speed thesis; short, clean package/Docker-image name. Sets package name + image name; ID prefixes stay semantic (`run_`, `pb_`), unaffected by the name. | 0 | D3 |
| 7 | 2026-06-16 | **API auth v1: `none`** (internal-network trust); `hmac` reserved | Conscious choice for the first internal-only deploy; `AuthGuard` is a pass-through until then. MUST be revisited before any public-facing deploy. | 0 | D1 |
| 8 | 2026-06-16 | **Sync mode: out** (async-only) for v1 | `POST /v1/runs` always returns `202`; results via webhook/poll. Simpler timeout + concurrency story. Revisit if callers need fast inline replays. | 0 | D2 |
| 9 | 2026-06-16 | **ID scheme: ULID**, prefixed `run_` / `pb_` | Lexicographically sortable, coordination-free, mature TS libraries; concrete ULID library chosen at Phase 1 (`ulidx`). | 0 | D4 |
| 10 | 2026-06-16 | **Dependency policy: pin latest-stable for every directly-declared package; latest-LTS Node** — verified at install and re-checked at every phase kickoff. Exceptions (documented): `@types/node` tracks the runtime Node major (not absolute latest); `playwright` npm is kept in lockstep with the `mcr.microsoft.com/playwright` image tag; versions nested *inside* Stagehand (its pinned `@anthropic-ai/sdk`, `pino`, `ai`, `openai`) are owned by Stagehand, not force-upgraded. | User directive: avoid the common AI-coding drift to stale versions. Latest **stable** only (never pre-release); pinned in `package.json` + lockfile for reproducibility; an automated currency check (Renovate/Dependabot or `npm outdated` in CI) can enforce it over time. Updates the "Node 22" part of #1 toward latest LTS — exact Node finalized at Phase 1 against the Node baked into the pinned Playwright image. | 0 | refines #1 |
| 11 | 2026-06-16 | **Model layer: provider-agnostic gateway** (refines #1's "AI sits behind the Claude API" framing — Anthropic is now one provider among several, not assumed). Day-one providers **Anthropic + OpenAI + Ollama/local + Google** via one `ModelGateway` on the Vercel AI SDK (`@ai-sdk/anthropic`/`openai`/`google`/`openai-compatible`), used by both the agent (Phase 4) and the surfaced replay fallback (Phase 5). Model chosen by a single **`model` string `provider/name`** (e.g. `openai/gpt-4.1`, `ollama/llama3.1`). **No built-in default — `model` is required** (`payload.config.model` > env `CONFIG_MODEL`); a run that needs a model with none set → `validation_error`. Provider/model selection is payload-overridable *behavior*; per-provider keys + endpoints (`ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `GOOGLE_GENERATIVE_AI_API_KEY`, `OLLAMA_BASE_URL`) are **env-only**. | User directive: no single-provider lock-in, and the engine must never silently pick a provider/model (cost + independence control). Replay stays zero-LLM regardless. Response contract (envelope/status/errors) **unchanged** — only `model` *config semantics* change. | 0 | refines #1 |
| 12 | 2026-06-16 | **Phase 1 stub uses `internal_error`, not `not_implemented`** — the skeleton's stubbed `POST /v1/runs` marks the run `failed` with `error.code: internal_error` + message "execution not implemented (Phase 1 skeleton)". `not_implemented` is deliberately **not** added to the spec §7 error-code set. | Keeps the locked error-code contract unchanged; the stub is transient (gone once Phase 4 execution lands). The phase-1 docs' `not_implemented` wording was a drift caught at kickoff. | 1 | — |
| 13 | 2026-06-16 | **`meta.effective_config` echoes resolved overridable behavior keys only** — self-heal flags, `model`, `run_timeout_seconds`, `agent_max_steps`, evidence/proxy/headless/offsite, fallback flags. Excludes secrets, destinations, and per-container capacity limits (the latter visible via `/v1/health`). | Clarifies the envelope contract: `effective_config` is the per-run behavior config, not container capacity or secrets. | 1 | — |
| 14 | 2026-06-16 | **`extract` op carries a `fields` map** (`output_format` field → selector relative to `scope_selector`); the structural extractor reads it deterministically (no per-field heuristics). Extends the op vocabulary (`DATA_MODEL.md` §4). | Structural extraction needs a field→element mapping; the documented op only had `scope_selector`. The mapping is **agent-determined on the first run (Phase 4) and compiled into the playbook**; hand-authored in Phase 2 fixtures. The two-speed thesis applied to extraction — the runner stays a deterministic reader. | 2 | — |
| 15 | 2026-06-17 | **`runs.result` column added** (migration `0002_run_result.sql`) to persist the extracted result. | `DATA_MODEL.md` §2 omitted `result`, but `GET /v1/runs/{id}` must return it on poll (the envelope's `result`). As-built schema correction; synced into `DATA_MODEL.md` §2 + `ARCHITECTURE.md` §5.1. | 2 | — |
| 16 | 2026-06-17 | **Compose Postgres host port → `5433`** (was 5432) to avoid clashing with a developer's local Postgres; the engine still connects in-network via `postgres:5432`. | A host Postgres on 5432 shadowed the container for host-side connections (integration tests/migrate). Host-only change; the cold-start gate is unaffected. | 2 | — |
| 17 | 2026-06-17 | **Concurrency limiter: `p-queue` (v9)** for the `MAX_CONCURRENT_RUNS` semaphore + the bounded waiting room; custom per-run wall-clock timeout-teardown + browser recycle wrapped around it. | A maintained library for the concurrency/queue core (with `size`/`pending` introspection feeding the 429 backpressure check + `/v1/health` saturation); the timeout/recycle/429 logic stays custom. | 3 | — |
| 18 | 2026-06-17 | **Queue-full → HTTP 429 is a transport rejection**: `{error:{message, retry_after_seconds}}` + a `Retry-After` header, **no run row and no new §7 error code**. | The §7 error-code set is reserved for run failures (`meta.error`); a 429 is intake backpressure like the existing 404/422 rejections. Response contract unchanged. | 3 | — |
| 19 | 2026-06-17 | **Browser recycle is verified via a monotonic `generation` counter, not the OS PID.** `BrowserPool` increments `generation` on each fresh Chromium launch; the recycle acceptance criterion ("PID changes after `BROWSER_RECYCLE_RUNS` runs") is met as "generation increments." The pool also keys browsers **per `headless` mode** (separate processes for headed/headless). | Playwright does not expose the launched Chromium's OS PID through its API, so a literal PID assertion isn't available; the generation counter is the recycle-observable proxy and is what the test asserts on. Internal mechanism only — no contract impact. | 3 | — |
| 20 | 2026-06-17 | **Isolation cross-contamination test runs HEAVY by default** — `ISO_CONCURRENCY=12 × ISO_ROUNDS=50 = 600` concurrent isolated runs, each stamping a distinct token into its own context's cookie + localStorage and reading back only its own; env-tunable to crank further in CI. | User directive: the project's single most important test should exercise real parallelism by default, not a token single-request assertion. It is the standing release-blocking regression gate (must stay green every later phase). | 3 | — |
| 21 | 2026-06-17 | **Phase 4 agent layer uses Stagehand v3.5 (latest stable), which owns its OWN CDP browser per run — not the Phase-3 Playwright `BrowserPool`.** Stagehand v3 is CDP-native (Playwright is only an optional peer dep) and rejects an externally-supplied Playwright `BrowserContext`/`Page` (upstream issue #1392), so each agent run launches one Stagehand instance in `env: LOCAL` (one instance = one isolated session), disposed at run end. Concurrency + wall-clock timeout still flow through the Phase-3 `Lifecycle` (teardown closes the Stagehand instance); the Playwright `BrowserPool` stays the deterministic **replay** path's browser. Provider-agnostic model selection (`provider/name`, incl. `agent()`) is native via the Vercel AI SDK. | Latest-stable policy (#10) points to v3.5; the plan's "agent runs on the Phase-3 pool" assumption is impossible with v3 — surfaced as a STOP-and-ask and resolved with the user (chose v3.5 over pinning the EOL-bound v2.5 line). Two browser mechanisms (Stagehand-CDP for learn, Playwright for replay), both isolated + both gated. Confirms #11 and retires the plan's "`agent()` may be provider-narrow" worry. Master-doc sync (ARCHITECTURE §3.4/§8) lands as-built at the Phase 4 gate. | 4 | refines #11; adjusts plan |
| 22 | 2026-06-17 | **Phase 4 verification posture: agent (learn) tests target Anthropic Claude Sonnet via env `ANTHROPIC_API_KEY`, opt-in (`test:live`); the real-site demo is deferred.** The offline suite stays LLM-free — the learn→replay round-trip's *replay* half is proven offline; the *learn* half (+ provenance / action-only / guardrail agent tests) run only under `test:live` with a key set. Phase 4 "complete" = fixture-complete (full automated agent→compile→replay on the bundled fixture); the "one real public site" exit demonstration is carried forward to run once a key/site is wired. | User directive: target Anthropic Sonnet, key from env only (never payload/chat), keep CI key-free. The real-site demo needs a live model + egress; deferring keeps the phase landable now while the fixture fully exercises the mechanism. Honors the require-explicit-`model` rule (#11). | 4 | — |
| 23 | 2026-06-17 | **Phase 4 implements `agent_initial` (fresh learn → new playbook) + `force_relearn` (→ new version); the `output_format`-only format-change-reuse optimization is deferred.** No `playbook_id` ⇒ learn a new playbook; `force_relearn` + an existing `playbook_id` ⇒ learn a new version (`addVersion`, `created_by: agent_initial`). The "update only the extraction step, reuse nav steps, re-invoke the agent only if new fields can't be located" optimization (PROJECT_SPEC §5.2) is **carried forward**. The `ModelGateway` lives at **`src/model/`** (postdates ARCHITECTURE §12, now synced). | Keeps Phase 4 landable fixture-complete; `force_relearn` already covers re-learning a changed task. The reuse path buys cost, not capability, and is best tuned against a live model. | 4 | — |
| 24 | 2026-06-17 | **Learn flow uses structured `act()` orchestration, NOT the autonomous `agent()`, for the recordable steps — live-verified GREEN.** Stagehand v3.5's `agent().execute()` records fills in `history` as value-less `click`s (verified live: compiled playbook had empty `required_data_keys`, no `{{data.*}}`). So the `AgentEngine` drives the form one field at a time via `act("Type <value> into the <key> field")` — Stagehand returns the operated selector and the data key is known directly (provenance **from the call**, ARCHITECTURE §4) — then `act` to submit, `extract` + per-field `observe` for the extraction selectors + a `wait_for` anchor. Also fixed: Anthropic base URL **normalized to `/v1`** (a conventional root `ANTHROPIC_BASE_URL` otherwise 404s `@ai-sdk/anthropic` at `/messages`); and the agent abort-signal needs Stagehand `experimental:true`+`disableAPI:true`. `provenance.ts`/`action-recorder.ts` are retained (spec §12 layout; value-identity + history mechanisms for a future autonomous-recording path) but not wired by the structured flow. | The autonomous agent doesn't yield a compilable selector+value stream; structured `act()` does, deterministically, and matches the project's core form-fill→extract shape. **Round-trip proven live:** learn→compile→replay-with-different-data passes (replay returns the NEW data → `{{data.*}}` parameterization correct). Economics: learn ≈ 12k input + 0.6k output tokens; replay = 0 LLM. | 4 | refines #22 |
| 25 | 2026-06-17 | **Phase 5 verification posture mirrors Phase 4:** offline proves the deterministic logic (heal §7 classification, surfacing flags, version TXN, unhealthy flagging) with the model stubbed; opt-in `test:live` proves mutate→heal→v2-replay + fallback-surfacing end-to-end (real Anthropic); AND an in-container run (`docker-agent-check` style). | User directive. Keeps CI key-free + deterministic while proving the model-using paths live and in the real container topology. | 5 | — |
| 26 | 2026-06-17 | **Extraction-miss handling: fallback-first-then-heal.** On a structural miss (≥1 field): if `REPLAY_LLM_FALLBACK=on` → one-shot LLM extract of the missing fields (surfaced); if STILL missing AND `self_heal_on_extraction_failure=on` → escalate to self-heal (re-learn `v(n+1)`); else → `completed_with_extraction_errors`. So `self_heal_on_extraction_failure` treats an extraction miss as a heal trigger, but only AFTER the cheap fallback. | Cheap one-shot rescue before an expensive re-learn; resolves the §7 `extraction_failed` vs `completed_with_extraction_errors` ambiguity for the replay path. Statuses/codes/surfacing unchanged. | 5 | clarifies §7 |
| 27 | 2026-06-17 | **LLM fallback uses a cheaper model (tests default `anthropic/claude-haiku-4-5`); the `ModelGateway` gains the real model call.** Promotes the Vercel AI SDK (`ai`) + `@ai-sdk/anthropic` (already transitive via Stagehand) to **direct deps**; the gateway builds an AI-SDK model + `generateObject` for the surfaced fallback — the one sanctioned LLM call on the replay path, via the gateway only (never importing a provider SDK in `execution/playbook/`). Require-explicit `REPLAY_LLM_FALLBACK_MODEL` (no default). | Extraction is simple → a cheaper/faster model fits the high-frequency fallback path. Realizes DECISIONS #11's gateway-on-AI-SDK design. | 5 | realizes #11 |
| 28 | 2026-06-17 | **Heal/fallback thresholds + metrics + reservation refactor.** `HEAL_FAILURE_THRESHOLD` (env, default 3) consecutive heal failures → `health=unhealthy` (surfaced via `?health=unhealthy`). Fallback engagements tracked as a **persistent per-playbook DB count** (new migration) feeding `FALLBACK_AS_DRIFT_SIGNAL=on` → flag for re-learn after K. **Prometheus export deferred** (no `metrics.ts` — observability/Phase 6). Also: the Phase-3 capacity **reservation is released once per run by the orchestrator** (not inside `lifecycle.execute`/`executeAgent`) so a self-heal's second gated execution doesn't double-release. | DB count gives durable drift visibility without standing up Prometheus now; the reservation refactor preserves the Phase-3 concurrency invariant across the replay→heal handoff. | 5 | — |
| 29 | 2026-06-17 | **Phase 6 emulates AWS with LocalStack (SQS + S3) in docker-compose** for the integration tests AND the `api`+`worker` end-to-end gate. AWS SDK v3 (`@aws-sdk/client-sqs`, `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`) honors a custom endpoint (`AWS_ENDPOINT_URL`/`S3_ENDPOINT`) so the same adapters hit LocalStack in dev and real AWS in prod. | No real AWS creds/cost, fully repeatable, matches the in-Docker verification bar from Phases 4–5. | 6 | — |
| 30 | 2026-06-17 | **Webhook signing is DEFERRED to Phase 7 (auth).** This phase delivers the envelope to `callback_url` (POST, retry 3× backoff, record `runs.webhook_status`) but **unsigned** — v1 is `API_AUTH_MODE=none` (no caller identity, #7), so a per-caller HMAC secret has nothing to key on. The `X-Engine-Signature` HMAC lands with caller auth in Phase 7. | User-approved scope call: signing without caller identity would be a single global secret of little value; better to ship it alongside the auth that gives it meaning. **Relaxes the Phase-6 "valid signature" acceptance criterion** — carried forward, recorded honestly (not silently dropped). | 6 | defers part of §criteria |
| 31 | 2026-06-17 | **Evidence URLs stay the stable engine path `/v1/runs/:id/evidence/:file` in ALL storage backends; in S3 mode the engine 302-redirects each request to a freshly-presigned S3 URL.** Refines the plan's "signed expiring URLs in the envelope." | Uniform `{meta}` contract across local↔s3 (the phase's byte-identical-contract goal), no stale/expired links for a caller that polls later, yet still S3-native delivery (the engine isn't a data proxy — it redirects). | 6 | refines plan |
